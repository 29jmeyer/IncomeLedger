import SwiftUI

// Shared UI constants used across multiple files.
let bubbleHorizontalPadding: CGFloat = 18

// Placement bubble insets (tune bottom to sit above the custom tab bar)
let saveViewBubbleInsets = UIEdgeInsets(top: 12, left: 24, bottom: 120, right: 24)
